.env
.DS_Store
build/
DerivedData/
.DerivedData/
# Generated by XcodeGen from project.yml — run `xcodegen generate`.
*.xcodeproj/
*.xcworkspace/xcuserdata/
# Local Developer ID signing override (maintainers only).
Config/Signing.local.xcconfig
.claude/
PLAN.md
*.html
__pycache__/
bench_*.py
injection_probe.py
*.profraw

# Local design prototypes (never committed — OSS repo)
prototypes/

# Local scratch media (demo clips, audio beds — never committed)
local-media/

# Local-only demo/debug seeding utility (launch-arg gated; not shipped).
InkIt/DebugSeed.swift

# Local-only social preview image (uploaded to GitHub repo settings manually)
.github/media/og.png
